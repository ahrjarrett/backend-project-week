type User {
  id: ID!
  username: String!
  email: String!
  createdAt: String!
  updatedAt: String!
  notes: [String]!
}

input UpdatedUser {
  username: String!
}

type Query {
  getMe: User!
}

type Mutation {
  updateMe(input: UpdatedUser!): User!
}
